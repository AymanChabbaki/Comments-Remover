export const metadata = {
  title: 'Live Demo',
  description: 'Click around a fully interactive comment moderation dashboard with sample data -- no signup, no Facebook access required.',
  alternates: { canonical: '/demo' },
};

export default function DemoLayout({ children }) {
  return children;
}
