export const metadata = {
  title: 'Live Demo — Real Comments, Moderated Live',
  description: 'Post a comment on our own Facebook Page and watch the real AI moderation pipeline handle it within seconds.',
  alternates: { canonical: '/live-demo' },
};

export default function LiveDemoLayout({ children }) {
  return children;
}
